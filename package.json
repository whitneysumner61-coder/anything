{
  "name": "pybridge-devos",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "build:app": "tsc -p tsconfig.app.json",
    "app": "npm run build && npm run build:app && node app/dist/app-server.js",
    "dev": "npm run build && npm run build:app && node app/dist/app-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.18.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^25.0.9",
    "typescript": "^5.5.0"
  }
}
